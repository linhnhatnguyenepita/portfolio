[defaults]
inventory = inventory.ini
host_key_checking = False
# The standalone 'yaml' stdout callback was removed in community.general 12.0.0.
# Use the built-in default callback with YAML-formatted task results instead.
callback_result_format = yaml
